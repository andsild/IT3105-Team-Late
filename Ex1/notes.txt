Logilab:
    vi har en liste med variabel-domener
        vi kan slaa opp paa variabler
        hvert domene har en ".hasChanged()"

    reduserer alle domener, saa bare henter tilfeldig fra hvert domene

    en constraint er et object:
        liste med variabler, operator, osv

        "for val in domain: if operator(val, domain) then remove"

        de bruker ogsaa null..

    
